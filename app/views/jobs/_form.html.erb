<div class="row">

  <div class="col-md-6">
    <div class="well">
      <%= bootstrap_form_for @job do |f| %>

      <% if current_user.errors.any? %>
          <div id="error_explanation" class="errors">
            <ul>
            <% current_user.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
            </ul>
          </div>
        <% end %>

        <%= f.text_field :title %>
        <%= f.text_field :company  %>
        <%= f.text_area :description %>
        <%= f.text_field :location %>
        <%= f.collection_select :category_id, Category.all, :id, :name, include_blank: 'Select Category' %>
        <%= f.collection_check_boxes :skill_ids, Skill.all, :id, :name, label: "Skills Needed" %>
        <%= f.fields_for :skills, @job.skills.build do |skills_fields| %>
          <%= skills_fields.text_field :name, label: "Another Skill", placeholder: "Add a new skill here...(optional)" %>
        <% end %>
        <%= f.url_field :url, placeholder: "optional..." %>
        <%= f.submit class: "btn btn-primary btn-outline btn-sm" %>
      <% end %>
    </div>
  </div>
</div>
