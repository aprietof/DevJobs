<div class="row">

  <div class="col-sm-8">
    <div class="well">
      <h1><%= @job.title %></h1>
      <p><%= @job.category.name %></p>
      <h3>Company:</h3>
      <p><%= fa_icon "industry" %> <%= @job.company %> | <%= fa_icon "map-marker" %> <%= @job.location %></p>
      <h4>Description:</h4>
      <p><%= @job.description %></p>

      <div class="row">
        <div class="col-sm-10">
          <small>Posted: <%= @job.posting_date %></small>
          <p>Company Url: <%= link_to @job.url, "http://#{@job.url}" %></p>
        </div>
        <div class="col-sm-2">
          <%= button_to "Apply", jobs_path, method: "get", class: "btn btn-primary btn-outline btn-md" %>
        </div>
      </div>
    </div>
  </div>

  <div class="col-sm-3">
    <div class="panel panel-default">
      <div class="panel-heading">Latest Jobs</div>
        <div class="panel-body">
          <% @jobs.each do |job| %>
            <small><%= job.location%></small> | <small><%= job.posting_date %></small>
            <h5><%= link_to job.title, job_path(job) %></h5>
          <% end %>
        </div>
    </div>
  </div>

</div>

<%= button_to "Back", jobs_path, method: "get", class: "btn btn-primary btn-outline btn-sm" %>
